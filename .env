mkdir -p configs
cat > configs/.env.example << 'EOF'
# Configurações do Banco de Dados
DB_DRIVER=mysql
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASS=password
DB_NAME=crm_erp_db

# Configurações do Servidor
PORT=8080
ENV=development
EOF

# Criar o arquivo .env real baseado no exemplo
cp configs/.env.example configs/.env
